#include <stdio.h>
/**
 * main - Prints reversed alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch;

for (ch = 'Z'; ch >= 'a'; ch--)
{
putchar(i);
}
putchar('\n');
return (0);
}
